---
meta:
  - title: Textinput Component
  - description: Textinput document, demo and API
headers:
  Cache-Control: no-cache
handle:
  source: "app/src/components/core/Textinput.tsx"
  componentName: "Textinput"
---

import Demo from "../../components/Demo";
import TextInputBasicDemo from "./TextInputBasicDemo";
import TextInputBackgroundDemo from "./TextInputBackgroundDemo";
import TextInputSizeDemo from "./TextInputSizeDemo";
import TextInputColorDemo from "./TextInputColorDemo";
import TextInputLabelDemo from "./TextInputLabelDemo";
import TextInputSectionDemo from "./TextInputSectionDemo";
import TextInputNoborderDemo from "./TextInputNoborderDemo";
import TextInputGhostDemo from "./TextInputGhostDemo";
import DebouncedInputBasicDemo from "./DebouncedInputBasicDemo";
import DebouncedInputWaitDemo from "./DebouncedInputWaitDemo";
import DebouncedInputDefaultValueDemo from "./DebouncedInputDefaultValueDemo";

# TextInput

## 基本

<Demo data={{ Component: TextInputBasicDemo }} />

### 边框颜色

`color`属性只作用于边框. 因此只要设置了`color`属性值, 即便没有设置`bordered`属性也会呈现边框.

<Demo data={{ Component: TextInputColorDemo }} />

## 尺寸

<Demo data={{ Component: TextInputSizeDemo }} />

## 标签和帮助文本

<Demo data={{ Component: TextInputLabelDemo }} />

## 左侧节和右侧节

<Demo data={{ Component: TextInputSectionDemo }} />

## 无边框

无边框模式下可能因为`TextInput`组件的背景色与父元素相同导致不是显而易见. 可以为父元素设置不同的背景色或者使用自定义样式设置背景色解决.

<Demo data={{ Component: TextInputNoborderDemo }} />

## 透明

透明模式下输入框无背景色. 考虑同时禁用`bordered`属性.

<Demo data={{ Component: TextInputGhostDemo }} />

## 自定义样式

通过`slots`样式槽, 可以为其内的元素设置自定义 css 类

<Demo data={{ Component: TextInputBackgroundDemo }} />

---

# DebouncedInput

`DebouncedInput`组件会延迟得到值, 对于根据用户输入执行后台查询这种场景尤其有用.

`DebouncedInput`的默认延迟等待时间是`500ms`, 延迟超时后才会触发`DebouncedInput`的`onChange`事件, 如果在等待期内用户再次输入则会取消等待, 输入后重新启动计时.

<Demo data={{ Component: DebouncedInputBasicDemo }} />

## 设置延迟时间

可以设置`wait`属性自定义延迟等待时间, 下面的例子延迟为 1 秒

<Demo data={{ Component: DebouncedInputWaitDemo }} />

## 初始值

可以使用`defaultValue`属性为组件提供初始值.

<Demo data={{ Component: DebouncedInputDefaultValueDemo }} />
