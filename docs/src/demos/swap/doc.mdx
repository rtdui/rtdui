---
meta:
  - title: Swap Component
  - description: Swap document, demo and API
headers:
  Cache-Control: no-cache
handle:
  source: "app/src/components/core/Swap.tsx"
  componentName: "Swap"
---

import Demo from "../../components/Demo";
import SwapFlipDemo from "./SwapFlipDemo";
import SwapIconRotateDemo from "./SwapIconRotateDemo";
import SwapIconDemo from "./SwapIconDemo";
import SwapIndeterminateDemo from "./SwapIndeterminateDemo";
import SwapInFormDemo from "./SwapInFormDemo";
import SwapTextDemo from "./SwapTextDemo";

# 使用

## 文本 Swap

<Demo data={{ Component: SwapTextDemo }} />

## 图标 Swap

<Demo data={{ Component: SwapIconDemo }} />

## 旋转特效

旋转特效会在fade 特效上叠加旋转特效.

<Demo data={{ Component: SwapIconRotateDemo }} />

## 翻转特效

Swap 的孩子可以是任意元素, 本例中使用了 div 元素作为孩子. 并给它设置了翻转特效

<Demo data={{ Component: SwapFlipDemo }} />

## 初始时可以设置中间态

要启用中间状态, `indeterminate`属性设置为`true`, 并且必须提供三个孩子元素, 第三个元素将作为初始时的中间态

> Tip: 一旦进行了点击则再也不会回到中间态.

<Demo data={{ Component: SwapIndeterminateDemo }} />

## Swap 可以作为 form 的控件使用

Swap 在 form 中作为表单控件的使用和 React 原生的`<input type="checkbox">`元素完全相同. 同样支持受控属性(`checked`)和非受控属性(`defaultChecked`)以及`onChange`事件

<Demo data={{ Component: SwapInFormDemo }} />
